- [x] Storage 已具备 schema version、迁移入口和损坏修复/回退策略
- [x] 配置结构演进不再只依赖 `normalizeDesktopConfig()` 单点硬兜底
- [x] IPC 已增加参数校验与统一错误转换，不再大量直接透传 payload 或 raw result
- [x] 现有 IPC 名称与前端调用保持兼容
- [x] `renderer.mjs` 进一步收敛为页面编排入口，细节逻辑继续下沉
- [x] 规则、筛选、财报和 AI 工作区入口行为无明显回归
- [x] AI 模块已从 `desktop/` 根目录下沉到专用目录
- [x] 聊天模式、builder 模式和 AI 工作区调用链在目录迁移后保持兼容
- [x] 本轮整改未破坏现有本地数据、桌面端启动入口和主要运行流程
- [x] 所有重构相关文件通过语法检查与诊断检查
