- [x] `daily` 模式在启动常驻后不会立即执行，而是等待到下一次有效设定时间
- [x] `interval` 模式仍保持启动后立即执行一轮，不出现行为回退
- [x] 界面可见当前是否常驻、当前调度模式和下一次预计执行时间
- [x] 最近一次运行摘要包含最近一次执行时间和结果摘要
- [x] 定时触发被跳过时，日志或状态中能明确区分“已有任务在运行”等原因
- [x] 用户点击 `停止` 后，界面能明确显示当前未常驻且不再保留下一次执行时间
- [x] 保存定时配置后，界面回显值与实际调度采用值一致
- [x] 相关诊断与回归验证覆盖 `interval`、`daily`、工作日限定和长任务跳过场景
