- [x] 渲染层主脚本回到编排定位，不再继续集中承载配置、结果表、AI 面板和规则编辑细节
- [x] 配置控制、结果表渲染、AI 面板、运行控制至少拆成独立模块并被主脚本装配使用
- [x] 规则编辑器的条件映射、modal 渲染和保存校验从主脚本中解耦
- [x] `main.mjs` 仅保留窗口生命周期、IPC 路由和必要协调，不再堆积业务判断
- [x] `preload.mjs` 暴露接口按职责分组且与主进程实现保持一致
- [x] AI 服务已形成独立分层，至少区分任务入口、执行器、结果归一和未来编排扩展点
- [x] 当前单模型 AI 解读链路保持可用，且未来智能体集群接入无需重写渲染层和主进程层
- [x] AI 配置结构具备扩展到多角色、多模型和多步骤编排的边界
- [x] AI 服务定义了结构化输出 schema，可覆盖规则、筛选和财报模板等自动填充场景
- [x] AI 结构化结果到页面表单状态的映射与校验层独立存在，不散落在页面按钮事件中
- [x] 规则、筛选和财报模板未来可接收智能体集群返回的结构化数据并安全自动填充
- [x] `engine.mjs` 中 universe 缓存、默认规则统计、财报统计、指标计算、规则执行、通知与调度至少完成领域拆分
- [x] 桌面端共享配置、共享运行时工具和错误包装维持单一来源，无新增复制实现
- [x] 现有配置字段、规则文件、状态文件和 IPC 名称保持兼容
- [x] 规则、筛选、财报、AI 解读、调度入口的用户可见行为无明显回归
- [x] 所有重构相关文件通过语法检查与诊断检查
