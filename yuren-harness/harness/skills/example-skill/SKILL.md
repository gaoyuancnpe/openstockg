---
name: "example-skill"
description: "技能编写示例:展示 SKILL.md 的结构约定(铁律/执行流程/输出规范)。做领域产品时照此结构写自己的技能,并把本示例删除或留作参考。"
---

# 技能编写示例

## 铁律(不可违反)
1. 用户未提供的关键数据,先列清单追问,禁止假设编造。
2. 关键结论必须来自工具或可靠来源,逐条附出处。
3. {{领域特有的强约束,如安全/合规前置}}

## 执行流程

### 第 1 步 信息采集与检查
收集并向用户确认:{{所需输入}}。缺项列清单追问。

### 第 2 步 {{核心处理}}
调用 {{工具}};返回 found=False 时明确告知,转 web_search 并标注置信度。

### 第 3 步 输出
生成结构化文档(存 workspace/outputs/),章节:{{约定章节}}。

---
技能约定:
- 目录结构 `<root>/<name>/SKILL.md`(发现深度只有一层);
- frontmatter 必须有 `name`(kebab-case)与 `description`(触发依据,写清"当用户……时使用");
- 领域产品把技能放 `harness/skills/`,启动器每次启动自动同步到数据目录的 `.dsh/skills/`(内容指纹不变则零磁盘操作)。
